
<%= form_for @debit, :html => { :class => 'form-horizontal' } do |f| %>  
  <% if @debit.errors.any? %>
    <div id="error_explanation">
      <h2>Eintrag konnte nicht erstellt werden! <%= @debit.errors.count %> Fehler wurden gefunden </h2>

      <ul>
      <% @debit.errors.full_messages.each do |msg| %>
      
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<link rel="stylesheet" href="http://code.jquery.com/ui/1.10.1/themes/base/jquery-ui.css" />
<script src="http://code.jquery.com/jquery-1.9.1.js"></script>
<script src="http://code.jquery.com/ui/1.10.1/jquery-ui.js"></script>
<script>

      
  $(function() {
 
    $( "#freund" ).autocomplete({
      minLength: 0,
      source: <%= raw "#{@Fullname.to_json}" %>,
     focus: function( event, ui ) {
        $( "#freund" ).val( ui.item.label );
        return false;
      },
      select: function( event, ui ) {
        $( "#email" ).val( ui.item.email );
        $( "#betrag" ).val( ui.item.label );
        return false;
      }
    })
    .data( "ui-autocomplete" )._renderItem = function( ul, item ) {
      return $( "<li>" )
        .append( "<a>" + item.label  +" " + item.icon + "</a>" )
        .appendTo( ul );
    };
  }); 
</script>


      <div class="control-group">
  <%= f.label :Freund, :class => 'control-label' %>
    <div class="controls">
    <%= f.text_field :firstname, :id => "freund", :class => 'text_field' %>
    
    </div>
  </div>
 

    <%= f.hidden_field :emailcurrentuser, :value => current_user.email, :class => 'text_field' %>
    <%= f.hidden_field :owner, :value => current_user.email, :class => 'text_field' %>
 

<br />

  <div class="control-group">
    <%= f.label 'Emailadresse', :class => 'control-label' %>
    <div class="controls">
    <%= f.email_field :emailuser2, :id => "email"%>
  </div>
  </div>

<br />

  <div class="control-group">
    <%= f.label :betrag, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :betrag, :class => 'text_field' %>
    </div>
  </div>

<br />

  <div class="control-group">
    <%= f.label :datum, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_select :datum, :class => 'date_select' %>
    </div>
  </div>

<br />

   <div class="control-group">
 <%= f.label :Info, :class => 'control-label' %>  
 <div class="controls">  
 <%= f.text_field :info, :id => "info" %>
 </div>
  </div>

<br />

  <div class="control-group">
    <%= f.label :art, :class => 'control-label' %>
    <div class="controls">
      <%= f.select :art, [['Verliehen','Verliehen'],['Geliehen','Geliehen']], :class => 'select' %>
    </div>
  </div>

<br />

  <div class="control-group">
    <%= f.label :gezahlt, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :gezahlt, :class => 'text_box' %>
    </div>
  </div>

<br />

  <div class="form-actions">
    <%= f.submit  'Erstellen', :class => 'btn btn-success' %>
 <%= link_to t('.zurück'), debits_path, :class => 'btn'  %>
  </div>
<% end %>
